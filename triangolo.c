#include <stdio.h>

int main()
{
    int x;
    int y;
    int z;
    printf("inserisci lato x");
    scanf("%d", &x);
    printf("inserisci lato y");
    scanf("%d", &y);
    printf("inserisci lato z");
    scanf("%d", &z);
    if((x+y)>z && (y+z)>x && (x+z)>y)
    {
        printf("può essere un triangolo\n");
    }
  
    else
    {
        printf("non può essere un triangolo\n");
    }
    printf("inserisci i lati\n");
    scan("%d %d %d", &x , &y , &z)
    if(x+y>z&&y+z>x&&x+z>y)
    {
    if(x==y||y==z|x==z)
    {
     printf("è equilatero\n");
    }
    else if (x==y||y==z||X==Z)
    {
    
    }
    }
}
